'use client';
import { auth } from '@/auth';
import React from 'react'

const CreateUser = () => {
    // const {data:session}= auth()l
  return (
    <div>CreateUser</div>
  )
}

export default CreateUser